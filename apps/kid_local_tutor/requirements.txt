# Core AI libraries
whispercpp>=0.2.0         # offline speech‑to‑text via whisper.cpp
llama‑cpp‑python>=0.2.0    # llama.cpp bindings for local LLM inference
piper‑tts>=0.1.0           # text‑to‑speech via Rhasspy Piper

# Semantic embeddings and retrieval
chromadb>=0.4.0            # local vector database for RAG
sentence‑transformers>=2.2 # MiniLM embedding model

# Web interface
streamlit>=1.20

# Multilingual support
langdetect>=1.0             # language detection for multilingual tutor
argostranslate>=1.8; extra == "multilingual"  # optional offline translation engine (see README)

# Data handling and utilities
numpy>=1.23
sqlalchemy>=2.0            # optional if using SQL backends
cryptography>=42.0         # encryption for journals

# Optional: safety and keyword detection
scikit‑learn>=1.3          # used for simple keyword extraction (optional)

# Note: Some packages compile native code.  On Raspberry Pi you may need to install build tools (gcc, cmake) before installation.